Quickstart:

Open and play this scene:
  `world` in `B3d/Demos/Btc/Ocean/Scenes/`

The build in app stores contains these two scenes:
  `loginscreen`
  `world`

The bare minimum build to retrieve graph data is in this scene:
  `minimal` in `B3d/Demos/Btc/Minimal/Scenes/`

See full developer documentation here: https://github.com/KevinSmall/blockchain3d/wiki